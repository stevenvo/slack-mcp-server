{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-10-17/server.schema.json",
  "name": "io.github.stevenvo/slack-mcp-server",
  "description": "MCP server for reading Slack conversations and threads. Access channel messages, threads, user info, and search conversations directly from your MCP client.",
  "version": "0.1.0",
  "repository": {
    "url": "https://github.com/stevenvo/slack-mcp-server",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "pypi",
      "registryBaseUrl": "https://pypi.org",
      "identifier": "stevenvo-slack-mcp",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "SLACK_USER_TOKEN",
          "description": "Slack User OAuth Token (starts with xoxp-). Get from https://api.slack.com/apps",
          "format": "string",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "SLACK_BOT_TOKEN",
          "description": "Slack Bot User OAuth Token (starts with xoxb-). Alternative to user token.",
          "format": "string",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "SLACK_WORKSPACE_URL",
          "description": "Your Slack workspace URL (e.g., https://your-company.slack.com). Required for generating message links.",
          "format": "string",
          "isSecret": false,
          "isRequired": true
        }
      ]
    }
  ]
}
